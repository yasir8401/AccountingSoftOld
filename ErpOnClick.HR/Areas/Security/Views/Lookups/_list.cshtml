@using ErpOnClick.DAL.Models
@{
    List<Lookups> CodeList = Model.CodeList;
    List<LookupTypes> LookupTypeList = Model.LookupTypeList;
}

<div class="table-responsive pb-2 ">
    <table id="dtList" class="table table-hover table-condensed ">
        <thead class="text-sm">
            <tr>
                <th style="width:25px">Code</th>
                <th>Name </th>
                <th>Arabic Name</th>
                <th>Type</th>
                <th style="width:30px">Status</th>
                <th style="width:15px"></th>
                <th style="        width: 15px"></th>

            </tr>
        </thead>
        <tfoot style="display:table-row-group">
            <tr class="text-center">
                <th>Id</th>
                <th>Name </th>
                <th>Arabic Name</th>
                <th>Type</th>
                <th>Status</th>
                <td></td>
                <td></td>

            </tr>
        </tfoot>
        <tbody>
            @foreach (var item in CodeList)
            {
                var LookupTypeDesc = LookupTypeList.Find(c => c.LookupTypeId == item.LookupTypeId);

            <tr class="text-center">
                <td>@item.Code</td>
                <td class="text-left">@item.LookupNameEn</td>
                <td class="text-right">@item.LookupNameAr</td>
                <td>@(LookupTypeDesc != null ? LookupTypeDesc.LookupTypeNameEn : "")</td>

                <td><i class="@(item.ActiveStatus==true?"fa fa-check":"")"></i></td>
                <td class="text-center padding-5">
                    <a href="~/Security/Lookups/edit/@item.Code" class="btn btn-default btn-block  btn-xs "><i class="fa fa-edit"></i></a>
                </td>
                <td class="text-center padding-5">
                    <a href="javascript:void(0)" class="btn btn-default btn-block btn-xs " onclick="deleteItem('@item.Code')"><i class="fa fa-times text-danger"></i></a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>


<script>
    $(function () {
        _initDatatable('#dtList');
    });

    function deleteItem(id) {
        confirmAction('Confirm ?', 'Are you sure you want to save changes ?', 'info', 'Yes', 'No').then(function (result) {
            if (result.value == true) {

                $.ajax({
                    url: getApplicationName() +"Security/Lookups/delete",
                    async: false,
                    type: "POST",
                    data: { id },
                    success: function (data) {
                        if (data.result.isError != true) {
                            showMsg('Deleted !', 'Record has been deleted !', 'success');
                            setTimeout(function () {
                                window.location = getApplicationName() +'Security/Lookups';
                            }, 500);
                        }
                        else if (data.result.msg != '') {
                            showMsg('Delete Failed !', data.result.msg, 'error');
                        }
                        else {
                            showMsg('Delete Failed !', 'Something went wrong. Try again', 'error');
                        }
                    }
                });
            }
        });
    }

</script>