@using ErpOnClick.DAL.Models
@using ErpOnClick.ErpMain.Resources
@inject LocService SharedLocalizer

@{
    List<FixedAsset> FixedAssetIdList = Model.FixedAssetIdList;

}

<div class="table-responsive pb-2 ">
    <table id="dtList" class="table table-hover table-condensed ">
        <thead class="text-sm ">
            <tr class="col-lg-12">
                <th style="width:20px">@SharedLocalizer.GetLocalizedHtmlString("Id")</th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Name")</th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Chart of Account")</th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Asset Type")</th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Purchase Date") </th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Warranty Date") </th>
                <th>@SharedLocalizer.GetLocalizedHtmlString("Purchase Price") </th>

                <th style="width:25px">@SharedLocalizer.GetLocalizedHtmlString("Edit")</th>
                <th style="width:25px">@SharedLocalizer.GetLocalizedHtmlString("Delete")</th>
            </tr>
        </thead>
        <tfoot style="display:table-row-group">
            <tr class="text-center">
                <th style="width:20px">ID</th>
                <th>Name</th>
                <th>Chart of Account</th>
                <th>Asset Type</th>
                <th>Purchase Date</th>
                <th>Warranty Date</th>
                <th>Purchase Price </th>

                <td></td>
                <td></td>
            </tr>
        </tfoot>
        <tbody>
            @foreach (var item in FixedAssetIdList)
            {
                <tr class="text-center">
                    <td style="width:20px">@item.FixedAssetId</td>
                    <td class="text-left">@(item.FixedAssetEn != null ? item.FixedAssetEn : "-")</td>
                    <td class="text-left">@(item.Account != null && item.Account.AccountNameEn != null ? item.Account.AccountNameEn : "-")</td>
                    <td>@(item.AssetType != null && item.AssetType.LookupNameEn != null ? item.AssetType.LookupNameEn : "")</td>
                    <td>@(item.PurchaseDate!= null ? Convert.ToDateTime(item.PurchaseDate).ToString("dd-MMM-yyyy") : "-")</td>
                    <td>@(item.WarrantyExpiry != null ? Convert.ToDateTime(item.WarrantyExpiry).ToString("dd-MMM-yyyy") : "-")</td>
                    <td class="text-right">@(item.PurchasePrice != 0 ? item.PurchasePrice : 0)</td>

                    <td class="text-center">
                        <a href="@Url.Content("~/")Finance/FixedAsset/edit/@item.FixedAssetId" class="btn btn-default btn-block  btn-xs "><i class="fa fa-edit"></i></a>
                    </td>
                    <td class="text-center">
                        <a href="javascript:void(0)" class="btn btn-default btn-block btn-xs " onclick="deleteItem('@item.FixedAssetId')"><i class="fa fa-times text-danger"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


<script>
    $(function () {
        $('#Accli').addClass("menu-open");
        $('#Reportsli').removeClass("menu-open");
        $('#Statementsli').removeClass("menu-open");
        $('#AccPli').removeClass("menu-open");
        $('#AccRli').removeClass("menu-open");
        $('#Setupli').removeClass("menu-open");

        _initDatatable('#dtList');
    });

    function deleteItem(id) {
        confirmAction('Confirm ?', 'Are you sure you want to save changes ?', 'info', 'Yes', 'No').then(function (result) {
            if (result.value == true) {

                $.ajax({
                    url: getApplicationName() + 'Finance/FixedAsset/delete',
                    async: false,
                    type: "POST",
                    data: { id },
                    success: function (data) {
                        if (data.result.isError != true) {
                            showMsg('Deleted !', 'Record has been deleted !', 'success');
                            setTimeout(function () {
                                window.location = getApplicationName() + 'Finance/FixedAsset';
                            }, 500);
                        }
                        else if (data.result.msg != '') {
                            showMsg('Delete Failed !', data.result.msg, 'error');
                        }
                        else {
                            showMsg('Delete Failed !', 'Something went wrong. Try again', 'error');
                        }
                    }
                });
            }
        });
    }

</script>